# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Logs / deps (in case a build is added later)
node_modules/
npm-debug.log*
yarn-error.log*

# Workspace scratch
screenshots/
*.tmp

# Dev-only live preview (transpiles .jsx in-browser; not part of the build)
preview.html
